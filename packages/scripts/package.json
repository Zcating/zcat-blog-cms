{
  "name": "scripts",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf ./build && tsc --project ./tsconfig.json && chmod +x ./build/bin/pm2-deploy.js"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  },
  "bin": {
    "pm2-deploy": "./build/bin/pm2-deploy.js"
  }
}